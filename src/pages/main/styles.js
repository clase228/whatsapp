import { styled } from "styled-components";

export const Container = styled.div`
   margin: 0 auto;
   width: calc(100% - 38px);
   max-width: 1600px;
   height: calc(100vh - 30px);
   padding-top:20px;
   display:flex;
`